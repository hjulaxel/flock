<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" width="16" height="16" role="img">
  <title>New session</title>
  <!-- Drawn as a filled shape, not a stroked one: the webview paints this as a
       CSS mask over the theme's icon foreground, and a mask reads ALPHA, so a
       fill gives it a solid, predictable one at every zoom level.
       NB. no double hyphen anywhere in this comment. An svg used as a CSS mask
       is parsed as strict XML, where a comment containing one is a fatal error
       and the whole file renders as nothing. -->
  <path d="M7.25 2.5h1.5v4.75H13.5v1.5H8.75V13.5h-1.5V8.75H2.5v-1.5h4.75z"/>
</svg>
